<div class="relative w-full h-32 bg-blue-900 flex items-center px-6">
  <h2 class="text-3xl md:text-4xl font-bold text-white">
    <%= announcement.title %>
  </h2>
</div>

<div class="container mx-auto p-8 bg-white rounded-lg shadow-md lg:-mt-44">
  <div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-4 mb-6">
    <div class="flex items-center bg-blue-100 p-2 max-w-max rounded-lg">
      <svg
        class="w-6 h-6 mr-2 text-blue-700"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
        />
      </svg>
      <span class="font-semibold text-lg text-blue-700"
        ><%= new Date(announcement.created_at).toLocaleDateString() %></span
      >
    </div>
  </div>

  <p class="mb-4"><%= announcement.description %></p>

  <p><%= announcement.content %></p>
  <p>
    unduh file <% if(announcement.file) { %><a
      class="text-blue-500"
      href="/<%= announcement.file %>"
      ><%= announcement.file %></a
    ><% } else { %>-<% } %>
  </p>
</div>
